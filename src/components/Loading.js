import React from "react";

const Loading = () => {
  return (
    <div className="ui active center loader">
      <div className="ui text loader">Loading</div>
    </div>
  );
};

export default Loading;
